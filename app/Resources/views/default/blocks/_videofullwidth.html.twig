{% import "_macro/_m-video-ratio.html" as mVideoRatio %}

{{ mVideoRatio.build({
    'title': block.getTitle(),
    'text': block.getSubtitle(),
    'video': vich_uploader_asset(block, 'videoFile'),
    'link': block.getLink(),
    'label': block.getLinkLabel(),
}) }}