{% import "_macro/_m-text-media.html" as mTextMedia %}

{{ mTextMedia.build({
    'title': block.getTitle(),
    'text': block.getSubtitle(),
    'media': vich_uploader_asset(block, 'videoFile'),
    'link': block.getLink(),
    'label': block.getLinkLabel(),
    'type': 'video',
    'align': 'left'
}) }}