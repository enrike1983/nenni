{% import "_macro/_m-full-img-text.html" as mFullBgText %}

{{ mFullBgText.build({
    'title': block.getTitle(),
    'text': block.getSubtitle(),
    'img': vich_uploader_asset(block, 'imageFile'),
    'link': block.getLink(),
    'label': block.getLinkLabel() ,
}) }}